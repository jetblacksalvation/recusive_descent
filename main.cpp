#include "recusive_parser/parser.hpp"
int main(void){
    // std::cout<< grammar_instances[intergral_prototype::name]->checkToken("12")<<": 12";
    NonTerminalGrammar* bruh = new NonTerminalGrammar;
    bruh->createChildren({});
    std::cout<<"running parser\n";
    return 0;
}